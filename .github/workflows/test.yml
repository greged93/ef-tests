name: test

on:
  workflow_call: {}

permissions: read-all

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-test
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.x
      - name: Setup rust env
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          components: llvm-tools-preview
          override: true
      - name: Retrieve cached dependencies
        uses: Swatinem/rust-cache@v2
      - name: fetch ef tests
        run: make setup
      - name: fetch Kakarot
        run: make setup-kakarot
        env:
          GITHUB_TOKEN: ${{ github.token }}
      - name: run tests
        run: |
          echo "$(make unit 2>&1 | base64 -w 0)" >> output
          exit_code=$?
          if [ $exit_code -ne 0 ]; then
            exit $exit_code
          fi
          echo "test-output=$output" >> $GITHUB_OUTPUT
        id: run_tests
      - name: get resources
        run: python scripts/compute_resources.py
        env:
          OUTPUT: ${{ steps.run_tests.outputs.test-output }} | base64 -d
      # upload artifacts
      - name: upload resources
        uses: actions/upload-artifact@v3
        with:
          path: ./resources.csv
          name: resources-usage
